# Payload-en-memoria
¿Qué es esto?  Este es un ejemplo para que entiendas cómo podés meter un payload (un código binario que hace algo) adentro de la sección `.rdata` de un ejecutable en C y después ejecutarlo en memoria. En este caso, el payload es un shellcode que abre la calculadora de Windows (`calc.exe`).
